/*
@author Nirupam Debnath
*/

pipeline {
  agent {
    kubernetes {
      inheritFrom "node22"
    }
  }
  stages {
    stage('Run maven') {
      steps {
        // container('maven') {
        //   sh 'mvn -version'
        //   sh ' echo Hello World > hello.txt'
        //   sh 'ls -last'
        // }
        container('node') {
          sh 'npm version'
        }
      }
    }
  }
}